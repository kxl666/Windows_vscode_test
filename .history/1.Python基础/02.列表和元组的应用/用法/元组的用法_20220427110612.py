# 列表,元组和字符串都可以使用 count index 方法

# 1.更新
# 1.1 元组不允许修改,只能通过切片来实现更新元组的目的,但是切片操作不会改变原来的元组,而是返回一个新的元组
# 1.2 列表,元组,字符串都可以通过 + 和 * 运算符来进行拼接
# 1.3 元组和字符串属于不可变类型,无法直接使用copy(),但是可以通过import copy来进行深或浅拷贝

# 2.删除
# 同样只能通过切片来实现删除元组的目的,但是切片操作不会改变原来的元组,而是返回一个新的元组
# 使用del来删除整个元组

# 3.切片
# 列表,元组,字符串都可以使用切片操作,因为它们都属于序列类型

# 4.索引
# 列表,元组,字符串都可以通过索引获取每一个元素
# 列表,元组,字符串都可以使用 count index 方法
